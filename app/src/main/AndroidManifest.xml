<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.co.muf.okta.androidpemulacourse">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".recycleview.activity.RecycleViewActivity"></activity>
        <activity android:name=".styletheme.StyleThemeActivity" />
        <activity android:name=".viewgroup.ViewGroupActivity" />
        <activity android:name=".intent.IntentActivity" />
        <activity android:name=".intent.MoveWithDataActivity" />
        <activity android:name=".intent.MoveActivity" />
        <activity android:name=".hitung.MainActivity" />
        <activity android:name=".home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>